# v0.0.1 — Unattended real-device implementation run

**Status:** Owner-authorized operating direction for the current v0.0.1 run.  
**Author:** Planner: SPEC Design  
**Tracker:** [#13](https://github.com/code2hack/EyeBrowse/issues/13)  
**Product and acceptance contract:** root `SPEC.md`, especially §§13–16.

## Authority and scope

The Owner approved removing avoidable human gates, then corrected the proposed emulator-only restriction: the real Fold6 stays plugged in and agents may operate it automatically. RG also remains connected. All project agents may capture and inspect screenshots from both devices whenever useful for EyeBrowse testing, debugging, or review. This file consolidates that final direction; the earlier emulator-only/no-real-Phone proposal is not current policy.

This is the existing run, not a new version, new Worker assignment, or reset of the implementation. Preserve product scope, existing agents, branches, candidate commits, recorded observations, and attempt counts. Do not merge unfinished code just to synchronize documentation. No release publication or release-build gate is authorized.

## Targets, operations, and screenshots

Real Fold6 and real RG are primary test/debug targets. A reserved, explicitly selected emulator is supplementary. Verify current ADB identity before commands; do not rely on a cached address or whichever device is first in a list. Manager coordinates exclusive mutation and existing claims.

Agents are authorized to install/debug the mission's EyeBrowse APKs, run scoped instrumentation/UI automation, navigate and type harmless test data, collect diagnostics, and capture/transfer/inspect/retain relevant screenshots through existing authorized ADB connections. No new Owner confirmation is required for routine operations or individual screenshots. Host builds, Git, fixture services, and analysis remain ordinary host-side work.

Prefer EyeBrowse and harmless fixture screens. Screenshot authorization does not grant permission to publish unrelated private content or credentials; crop/redact incidental sensitive content before public issue/PR attachments. Do not bypass OS screenshot restrictions or access unrelated apps merely to collect data. Screenshots establish rendering/layout, not optical comfort, timing, or human attribution by themselves.

Keep USB/ADB available. Do not revoke debugging, disable authentication or secure lock, reboot/reset devices, disconnect cables, or destabilize the sole management route as a test shortcut. Prefer app-scoped stop/restart and app-link fault injection. A secure-lock test requires a safely recoverable authorized setup; never request/store the Owner's unlock secret or confuse screen-off with secure lock.

## Acceptance without physical waits

Follow the current unattended acceptance matrix in SPEC §15. Test real application behavior with ADB-driven UI/instrumentation, not bypasses that directly fabricate success. Camera-image and sensor replay inputs may be confined to test-only seams and must exercise the real decoder/controller, authenticated protocol, and downstream browser behavior.

Physical-only observations unavailable without the Owner are non-blocking for this development run and must be recorded as `NOT EXERCISED — unattended profile`. This includes wearer/optical judgment, physical hinge movement, optical QR alignment, unplugged power conditions, and network/lock setup requiring unavailable physical action or private authentication. Do not hide them as unchanged blockers in #12. Do not call them passed, and do not excuse an actual failed software test or missing required implementation under this rule.

After required automated checks, independent exact-head review, and verified cleanup, tickets may close and dependencies may advance. #12 closes the unattended development profile with evidence and limitations; it is not full physical qualification or release approval. Preserve prior valid real-device and Owner observations with their actual build and source; do not demand unnecessary repeats.

## Manager coordination

Refresh current remote governance, tracker #13, issue/PR state, registry, and live routing. Reuse the existing run's no-numeric-cap concurrency authorization subject to resources and batch rules. Request updated detailed ticket plans from the registered version Planner where earlier plans demand obsolete manual steps. This agent-to-agent planning is not a human gate.

Relay this direction directly to every active agent, confirm receipt, and pass it to later assigned agents. A GitHub comment is not receipt confirmation. Reconcile run records and the actual remaining acceptance work before adjusting labels. Resume existing assignments; do not dispatch a duplicate Worker. Assigned/in-review work normally has no ready label. Keep genuine prerequisite edges and current batch closeout rules.

Required independent review, bounded Worker/Manager recovery, simplification, verification, cleanup, and protected-article publication gates remain in force. Ordinary implementation may be accepted and merged through the existing workflow without waking the Owner. This run record does not authorize editing AGENTS.md or DEV.md; operational details can be recorded in plans, issues, and evidence.

## Quiet blockers

The Owner is sleeping: no audible alarms and no expectation of immediate manual action. Record necessary escalation in writing with the affected ticket, evidence, safe state, and precise decision/access needed. Missing credentials, essential ADB/device access, required sudo, applicable quota restrictions, both approved availability routes failing, or exhausted recovery budgets are real blockers, not reasons to bypass safeguards or reset counts. Continue independent authorized work only where current dependencies and batch policy permit.

## Evidence references

- [Original unattended audit](https://github.com/code2hack/EyeBrowse/issues/13#issuecomment-5621910341): historical proposal; its emulator-only target restriction is superseded.
- [Final real-Phone correction and screenshot authorization](https://github.com/code2hack/EyeBrowse/issues/13#issuecomment-5622213785).
- Owner subsequently requested publication of the consolidated direction to main and a direct Manager handoff prompt.
