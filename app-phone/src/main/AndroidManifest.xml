<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The Phone browser is a network client; hosting adds only ordinary lifetime capabilities. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.EyeBrowsePhone"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic is the documented allowance that lets an explicitly typed http://
          address load. It is a browsing capability, not a future Phone-RG transport decision.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Non-exported foreground shell for an explicitly started hosting session. specialUse is
          the documented type for this offscreen browser-rendering slice; no boot/remote start and
          no exported command surface exist.
        -->
        <service
            android:name=".HostingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Offscreen hosting of the user's own browser session for presentation on a private virtual display" />
        </service>
    </application>

</manifest>
